type Note @model @auth(rules: [{allow: public}]) {
  id: ID!
  title: String
  text: String
}

type Medication @model @auth(rules: [{allow: public}]) {
  id: ID!
  medName: String
  medQuanitity: Int
  daysLeft: Int
  medStrength: Int
  dailyDose: Int
}
 